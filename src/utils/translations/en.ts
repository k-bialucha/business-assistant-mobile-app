/**
 * Because english is basic app language, translations' keys are english real text
 * that cause we don't need to write every translation, just the long ones which will have
 * key as text shortcut
 *
 */

import { Translation } from './types';

export const translationEN: Partial<Translation> = {
  // eg. 'Lorem ipsum...': 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor',
};
