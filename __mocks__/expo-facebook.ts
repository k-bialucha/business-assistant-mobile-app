export const logInWithReadPermissionsAsync = () => {
  return new Promise(resolve => {
    resolve();
  });
};

export const initializeAsync = () => {
  return new Promise(resolve => {
    resolve();
  });
};
